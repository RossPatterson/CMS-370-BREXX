/* DIGITS */
say 'Testing DIGITS ...'
fail_count=0
/* These from TRL2. */
if digits() \= 9 then call test_failed '1'
/* These from Mark Hessling. */
say '... DIGITS done'
exit fail_count
test_failed:
say 'failed in test' arg(1)
fail_count=fail_count+1
return
